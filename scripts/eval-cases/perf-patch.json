[
  {
    "id": "perf_compaction_params",
    "description": "性能补丁参数已正确写入 compaction.ts",
    "tags": ["perf", "regression"],
    "prompt": "读取 packages/opencode/src/session/compaction.ts，告诉我 PRUNE_MINIMUM 和 PRUNE_PROTECT 的当前值是多少",
    "dir": "../..",
    "timeout": 60,
    "assertions": [
      { "type": "no_error" },
      { "type": "output_contains", "pattern": "10[,\\s]?000|10000|10_000", "flags": "i" },
      { "type": "output_contains", "pattern": "20[,\\s]?000|20000|20_000", "flags": "i" }
    ]
  },
  {
    "id": "perf_truncation_params",
    "description": "性能补丁参数已正确写入 truncate.ts",
    "tags": ["perf", "regression"],
    "prompt": "读取 packages/opencode/src/tool/truncate.ts，告诉我 MAX_LINES 和 MAX_BYTES 的当前值",
    "dir": "../..",
    "timeout": 60,
    "assertions": [
      { "type": "file_exists", "path": "packages/opencode/src/tool/truncate.ts" },
      { "type": "no_error" },
      { "type": "output_contains", "pattern": "1000", "flags": "i" },
      { "type": "output_contains", "pattern": "25[,\\s]?\\*?\\s?1024|25600|25_600", "flags": "i" }
    ]
  },
  {
    "id": "perf_overflow_param",
    "description": "COMPACTION_BUFFER 已设置为 30K",
    "tags": ["perf", "regression"],
    "prompt": "读取 packages/opencode/src/session/overflow.ts，告诉我 COMPACTION_BUFFER 的值",
    "dir": "../..",
    "timeout": 60,
    "assertions": [
      { "type": "no_error" },
      { "type": "output_contains", "pattern": "30[,\\s]?000|30000|30_000", "flags": "i" }
    ]
  }
]
